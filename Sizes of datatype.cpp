
#include <stdio.h>

int main()
{
    printf("Storage size for Int is: %d byte \n", sizeof(int));

    printf("\nStorage size for Char is: %d byte \n", sizeof(char));

    printf("\nStorage size for Double is: %d byte \n", sizeof(double));

    printf("\nStorage size for Float is: %d byte \n", sizeof(float));

    printf("\nStorage size for Short is: %d byte \n", sizeof(short));

	printf("\nStorage size for Long is: %d byte \n", sizeof(long)); 

    return 0; 
}
